eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    enabled: true
    service-url: 
      defaultZone: http://localhost:8761/eureka
    instance:
      hostname: localhost
management:
  zipkin:
    tracing:
      endpoint: http://localhost:9941
